1.Set集合是一个接口，与List类似，都需要实现类来对其进行操作
    Set集合的特点，不能包含重复的值
    没有索引：代表不能用普通的for循环进行遍历，只能用迭代器或者增强for对其遍历
    不保证读写顺序

2.哈希值：哈希值就是JDK根据对象的地址或者字符串或者数值计算出来的int类型的数值
  通常情况下，不同对象的哈希值是不同的,相同的对象多次调用hashCode()方法得到的返回值是一样的

3.去重是通过hashCode和equals方法，必要时可以重写
